{
  "private": true,
  "main": "server.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview --host --open"
  },
  "typeRoots": [
    "node_modules/@types",
    "src/@types"
  ],
  "dependencies": {
    "babylonjs": "^6.9.0"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "vite": "^4.3.9"
  }
}
