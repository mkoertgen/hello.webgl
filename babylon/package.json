{
  "private": true,
  "main": "server.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview --host --open"
  },
  "typeRoots": [
    "node_modules/@types",
    "src/@types"
  ],
  "dependencies": {
    "babylonjs": "^5.52.0"
  },
  "devDependencies": {
    "typescript": "^5.0.3",
    "vite": "^4.2.1"
  }
}
